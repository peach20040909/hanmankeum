# GitHub 토큰: 비공개 저장소 수집 · API 한도(시간당 60→5000회) 확대. 권한은 읽기 전용이면 충분
GITHUB_TOKEN=
# 있으면 Claude가 작업 기록을 기여 유형으로 분류, 없으면 키워드 규칙 분류
ANTHROPIC_API_KEY=
PORT=3000
# Notion 연결의 내부 통합 시크릿 (페이지·사용자 조회)
NOTION_TOKEN=
# 웹훅 구독 생성 시 서버 로그에 찍히는 verification_token
NOTION_WEBHOOK_SECRET=
