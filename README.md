# 한만큼

> 각자가 한 만큼의 기록을 모아, 공정하게 참여한 만큼 평가받을 수 있도록 — Team HMK

흩어진 팀 프로젝트 작업 기록(GitHub · Notion · Docs)을 모아, 교수님이 **근거와 함께** 기여를 확인하는 웹 서비스입니다.

## 흐름

| 시점 | 교수 | 학생 |
|---|---|---|
| 착수 | 팀 생성 → 팀원별 초대 링크 전달 → 4축 가중치 설정 → 학생 의견 반영/기각 → 잠금 | 개인정보 수집·이용 동의 → 가중치 확인 · 의견 제출 |
| 학기 중 | 기록 수집 (점수 비공개) | 평소처럼 작업 · 알림·점수·순위 없음 |
| 마감 후 | 기여율 리포트 · 근거 확인 · 축 조정 · 동료 증언 확인 · PDF | 자기 기술 1회 · 익명 동료평가 1회 |

## 기여 4축과 계산

| 축 | 정의 | 예시 |
|---|---|---|
| 산출물 생성 | 새 결과물을 만듦 | 문서 작성, 커밋, 슬라이드 추가 |
| 산출물 개선 | 남의 결과물을 수정·보완 | 문서 편집, PR 리뷰, 오탈자 수정 |
| 조율/관리 | 팀 진행을 움직임 | 일정 등록, 태스크 배분, 이슈 |
| 의사소통 | 논의 참여 | 댓글, 피드백 |

- `r(i,k) = 개인 i의 축 k 활동량 / 팀 전체 축 k 활동량` (축마다 팀원 합 = 1.0)
- `기여율(i) = Σ_k [ r(i,k) × w(k) ] × 100%` (팀 전체 합 = 100%)
- 성적 변환·상하한·반영강도 조정 없음. 팀 전체에 활동이 0건인 축은 상대비율이 정의되지 않아 그 가중치를 나머지 축에 비례 배분(팀 합 100% 유지)
- 가중치 프리셋 (생성/개선/조율/소통): 개발 40/30/15/15 · 기획·조사 35/20/25/20 · **균형 30/25/25/20 (기본값)**
- 자기 기술·동료평가는 기여율에 **반영하지 않음** (교수 참고용, 리포트에서 수치와 구분 표시). 동료평가의 "동일 작업" 연결로 로그와 중복 가산하지 않음

**본 리포트는 성적을 산출하지 않습니다. 최종 평가는 교수님의 판단에 따릅니다.**

## 권한 (초대 링크 방식)

- 팀을 만들면 **교수 링크**와 **팀원별 초대 링크**가 생성됩니다. 링크의 키로 서버가 역할을 확인합니다.
- 교수만: 가중치 설정·잠금, 의견 반영/기각, 기록 수집·조정, 리포트, 마감, 삭제
- 학생: 동의 전에는 다른 기능 차단 · 본인 의견/자기 기술만 보임 · 학기 중 기록·점수 비공개
- 익명 동료평가: 팀원에게 응답자 비공개, 계정과 연결(중복 제출 방지·소명), 교수는 응답자 확인 가능 — 제출 전 화면에 명시

## 실행

Node.js 22.13 이상 (내장 SQLite 사용)

```bash
npm install
cp .env.example .env   # 선택: GITHUB_TOKEN, ANTHROPIC_API_KEY, NOTION_*
npm start              # http://localhost:3000
npm test
```

- `GITHUB_TOKEN` — 비공개 저장소 수집, API 한도 확대 (없으면 공개 저장소만, 시간당 60회)
- `ANTHROPIC_API_KEY` — Claude로 작업 기록을 4축에 분류 (없으면 규칙 분류)
- `NOTION_TOKEN`, `NOTION_WEBHOOK_SECRET` — Notion 웹훅 수집 (아래 참고)

## Notion 웹훅 수집

Notion API 조회는 블록마다 **마지막 편집자**만 알려주고 편집 이력은 제공하지 않습니다. 그래서 착수 시점부터 웹훅 이벤트의 `authors`를 쌓아 편집자별 기록을 만듭니다.

| 이벤트 | 축 |
|---|---|
| `page.created` | 산출물 생성 |
| `page.content_updated` | 본인이 만든 페이지면 생성, 남의 페이지면 개선 |
| `page.properties_updated` | 조율/관리 (일정·담당·상태) |
| `comment.created` | 의사소통 (연결에 "댓글 읽기" 기능 필요) |

1. https://app.notion.com/developers/connections 에서 연결 생성 → 시크릿을 `NOTION_TOKEN`에 등록 (기능: 콘텐츠 읽기, 댓글 읽기, 사용자 정보 읽기(이메일 포함))
2. 연결의 **Webhooks** 탭 → 구독 생성 → URL `https://<배포 주소>/api/webhooks/notion`, 위 4개 이벤트 선택
3. 서버 로그의 `[Notion] verification_token: ...` 값을 Notion **Verify**에 붙여넣고, 같은 값을 `NOTION_WEBHOOK_SECRET`에 등록 후 재배포
4. 착수 설정에서 팀 Notion 페이지 링크(교수)와 Notion 이메일(각자) 입력, 팀 페이지를 연결에 공유

- 같은 페이지 · 같은 편집자 · 같은 날(KST) · 같은 이벤트는 1건으로 합칩니다.
- 편집한 문서의 본문, 연결 이전 편집은 수집하지 않습니다. 기준 잠금 후 · 마감 전 이벤트만 기록합니다.

## 본선 시연

1. 첫 화면 → **시연용 예시 팀 불러오기** → 마감된 팀 3 리포트 (기여율 · 동료 증언 · 자기 기술)
2. 실제 흐름: **새 팀 만들기** → 착수 설정의 **이 학생으로 보기**로 학생 동의·확인·의견 → 교수 화면에서 반영 → 잠금 → **지금 마감 처리** → 학생 화면 동료평가 → 교수 리포트

## 구조

```
server.js    HTTP API + SQLite (node:sqlite), 초대 키 권한
collect.js   4축 정의 · GitHub 수집 · Claude/규칙 분류
notion.js    Notion 웹훅 서명 검증 · 페이지/사용자 조회
score.js     기여율 산식
seed.js      시연용 예시 데이터
public/      화면 (Vanilla JS)
```

## 아직 없는 것 (멘토링 후 결정)

- 학교 SSO 로그인 — 지금은 초대 링크 키
- Google Docs 자동 수집 — 지금은 링크 기록
- 낮은 기여자 별도 표시, 학생 본인 리포트 열람, 모바일 최적화
